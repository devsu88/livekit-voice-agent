[project]
name = "livekit-voice-agent"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "livekit-agents[cartesia,deepgram,openai,silero,turn-detector]~=1.2",
    "livekit-plugins-noise-cancellation~=0.2",
    "python-dotenv>=1.2.1",
    "openmeteo-requests>=1.7.4",
    "requests-cache>=1.0.0",
    "retry-requests>=0.2.0",
]
